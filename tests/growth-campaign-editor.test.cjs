const {test}=require('node:test'),assert=require('node:assert/strict');
global.CampaignContract=require('../n8n/growth/campaign-contract');
const E=require('../growth-campaign-editor');
const d={schema_version:'crm-campaign-v1',brand:'fish',channel:'email',initiative:{key:'fish-cliente',name:'Dia do Cliente'},utm_campaign:'fish-cliente',name:'Abertura',subject:'Kit',from_email:'Fishermans <contato@fishermans.com.br>',reply_to:'contato@fishermans.com.br',list_ids:[125,126],template_id:1,html:'https://fishermans.com.br/products/kit {{ UnsubscribeURL }}',text:'https://fishermans.com.br/products/kit {{ UnsubscribeURL }}',tags:['quentes'],send_at:'2026-09-20T18:00:00-03:00'};
test('AI import and manual form export produce the identical normalized definition',()=>{const form=E.fromDefinition(d);assert.deepEqual(E.definition(form),CampaignContract.normalize(d));assert.equal(form.list_ids,'125, 126');});
test('unknown imported attributes, including accidental auth properties, are not retained in drafts',()=>{const form=E.fromDefinition({...d,k:'should-not-persist',token:'should-not-persist',secret:'should-not-persist'});assert(!JSON.stringify(form).includes('should-not-persist'));assert(!JSON.stringify(E.definition(form)).includes('should-not-persist'));});
test('invalid imported content and out-of-scope channels fail before changing a draft',()=>{assert.throws(()=>E.fromDefinition({...d,channel:'whatsapp'}),{code:'CHANNEL_UNAVAILABLE'});assert.throws(()=>E.fromDefinition({...d,html:'<script>bad()</script>{{ UnsubscribeURL }}'}),{code:'HTML_ACTIVE_CONTENT'});});
