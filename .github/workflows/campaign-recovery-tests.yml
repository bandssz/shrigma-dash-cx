name: Campaign draft recovery
on:
  pull_request:
    paths:
      - 'n8n/growth/campaign-*'
      - 'tests/campaign-*'
      - 'tests/fixtures/campaign-*-pre-recovery.sql'
      - 'growth-campaign-*.js'
      - 'campaign-contract.js'
      - '.github/workflows/campaign-recovery-tests.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  isolated-postgres:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    services:
      postgres:
        image: postgres:17.10
        env:
          POSTGRES_DB: campaign_recovery_test
          POSTGRES_USER: synthetic
          POSTGRES_HOST_AUTH_METHOD: trust
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U synthetic -d campaign_recovery_test"
          --health-interval 5s --health-timeout 5s --health-retries 10
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install pinned isolated test dependencies
        run: npm install --prefix ../campaign-recovery-tools --no-save --ignore-scripts --no-audit --no-fund pg@8.13.1 @electric-sql/pglite@0.3.14
      - name: Upgrade and recovery guards
        env:
          NODE_PATH: ../campaign-recovery-tools/node_modules
        run: node tests/campaign-recovery-postgres.cjs
      - name: Independent transactions and lost response
        env:
          NODE_PATH: ../campaign-recovery-tools/node_modules
          TEST_DATABASE_URL: postgres://synthetic@localhost:5432/campaign_recovery_test
          CAMPAIGN_TEST_DATABASE_ISOLATED: '1'
        run: node tests/campaign-recovery-concurrency-postgres.cjs
